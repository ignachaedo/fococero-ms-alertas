apiVersion: v1
kind: Service
metadata:
  name: ms-alertas
  namespace: fococero
spec:
  selector:
    app: ms-alertas
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003
  type: ClusterIP
